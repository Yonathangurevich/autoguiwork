// should add functions from apps - if the i could build steps, i should have the option to create functions
// like openning the explorer or open praiorty, renaming file from downloads to other dir... etc.

struct Functions<T> {
    position: u32,
    functions: Vec<T>
}